int Solution::lengthOfLastWord(const string A) {
    int i=A.length()-1, ans=0;
    while(i>=0 && A[i]==' '){
        i--;
    }
    while(i>=0 && A[i]!=' '){
        ans++;
        i--;
    }
    return ans;
}
